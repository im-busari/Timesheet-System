import styled from "styled-components";

export const StyledButton = styled.button`
  color: var(--white);
  height: fit-content;
  padding: 0.5rem;
  margin-left: 1rem;
  cursor: pointer;
  border: none;
  border-radius: 6px;
  :focus {
    outline: none;
  }
`;
